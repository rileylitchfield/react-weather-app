import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rileywebdev.testing',
  appName: 'react-weather-app-mobile',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
